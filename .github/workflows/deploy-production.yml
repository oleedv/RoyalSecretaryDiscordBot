name: Deploy to Production

on:
  pull_request:
    branches: [production]
    types: [closed]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/royalsecretarydiscordbot

jobs:
  build:
    name: Build & Push Image
    if: github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:prod-${{ github.sha }}

  deploy:
    name: Deploy to Server
    needs: build
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ~/RoyalInfrastructure

            echo ${{ secrets.GHCR_TOKEN }} | docker login ghcr.io -u ${{ github.repository_owner }} --password-stdin

            cat > .env.prod << EOF
            GHCR_IMAGE=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
            BOT_IMAGE_TAG_PROD=latest
            DISCORD_TOKEN=${{ secrets.DISCORD_TOKEN }}
            DISCORD_CLIENT_ID=${{ secrets.DISCORD_CLIENT_ID }}
            DB_USER=${{ secrets.DB_USER }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            MARIADB_ROOT_PASSWORD_PROD=${{ secrets.DB_ROOT_PASSWORD }}
            SQUADJS_SERVERS=${{ secrets.SQUADJS_SERVERS }}
            BM_TOKEN=${{ secrets.BM_TOKEN }}
            BM_SERVER_ID=${{ secrets.BM_SERVER_ID }}
            EOF

            docker compose -f docker-compose.prod.yml --env-file .env.prod pull royal-secretary-bot
            docker compose -f docker-compose.prod.yml --env-file .env.prod up -d royal-secretary-bot
        env:
          REGISTRY: ghcr.io
          IMAGE_NAME: ${{ github.repository_owner }}/royalsecretarydiscordbot

  health-check:
    name: Health Check
    needs: deploy
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Wait for startup
        run: sleep 15

      - name: Verify container is running
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            STATUS=$(docker ps --filter name=royal-secretary-bot-prod --format "{{.Status}}")
            echo "Container status: ${STATUS}"

            if [ -z "$STATUS" ]; then
              echo "Container is not running!"
              docker logs --tail 30 royal-secretary-bot-prod 2>&1 || true
              exit 1
            fi

            echo "Container is healthy"

  notify:
    name: Notify Discord
    needs: [build, deploy, health-check]
    if: always() && (github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Send webhook
        run: |
          if [ "${{ needs.build.result }}" = "success" ] && \
             [ "${{ needs.deploy.result }}" = "success" ] && \
             [ "${{ needs.health-check.result }}" = "success" ]; then
            COLOR=3066993
            STATUS="Deployed"
          else
            FAILED=""
            [ "${{ needs.build.result }}" != "success" ] && FAILED="Build"
            [ "${{ needs.deploy.result }}" != "success" ] && FAILED="${FAILED:+$FAILED, }Deploy"
            [ "${{ needs.health-check.result }}" != "success" ] && FAILED="${FAILED:+$FAILED, }Health Check"
            COLOR=15158332
            STATUS="Failed (${FAILED})"
          fi

          COMMIT_MSG=$(echo '${{ github.event.pull_request.title }}' | jq -Rs .)

          curl -s -H "Content-Type: application/json" -d "{
            \"embeds\": [{
              \"title\": \"${STATUS}\",
              \"color\": ${COLOR},
              \"fields\": [
                { \"name\": \"Environment\", \"value\": \"Production\", \"inline\": true },
                { \"name\": \"Commit\", \"value\": \"[\`${GITHUB_SHA::7}\`](https://github.com/${{ github.repository }}/commit/${GITHUB_SHA})\", \"inline\": true },
                { \"name\": \"By\", \"value\": \"${{ github.actor }}\", \"inline\": true },
                { \"name\": \"Message\", \"value\": ${COMMIT_MSG} }
              ],
              \"timestamp\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"
            }]
          }" "${{ secrets.DISCORD_DEPLOY_WEBHOOK }}"
